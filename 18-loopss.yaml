- name: installing packages
  hosts: [web]
  tasks:
   - name: install packages
     ansible.builtin.package:
       name: "{{item}}"
       state: present
     loop:
      - mysql
      - nginx
      - nodejs        